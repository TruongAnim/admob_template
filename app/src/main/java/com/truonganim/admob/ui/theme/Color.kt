package com.truonganim.admob.ui.theme

import androidx.compose.ui.graphics.Color

// App Brand Colors
val BrandRed = Color(0xFFF82F4C)
val BrandRedLight = Color(0xFFFF5A73)
val BrandRedDark = Color(0xFFD91E3A)

// Main App Colors
val AppDarkBackground = Color(0xFF0F1112)      // Main dark background
val AppWhite = Color(0xFFFFFFFF)               // White
val AppImageBackground = Color(0xFF171021)     // Background for image screens (dark)
val AppLightImageBackground = Color(0xFFF5F5F7) // Background for image screens (light)

// Light Theme Colors (for future use)
val LightBackground = Color(0xFFFFFFFF)
val LightSurface = Color(0xFFF5F5F5)
val LightSurfaceVariant = Color(0xFFEEEEEE)
val LightOnBackground = Color(0xFF0F1112)
val LightOnSurface = Color(0xFF1C1C1C)
val LightOutline = Color(0xFFE0E0E0)

// Dark Theme Colors (Main theme)
val DarkBackground = AppDarkBackground         // #0F1112
val DarkSurface = Color(0xFF1A1B1C)           // Slightly lighter than background
val DarkSurfaceVariant = Color(0xFF252627)    // Even lighter for cards
val DarkOnBackground = AppWhite                // White text on dark
val DarkOnSurface = Color(0xFFE8E8E8)         // Slightly dimmed white
val DarkOutline = Color(0xFF3A3B3C)           // Subtle borders

// Image Screen Background
val ImageScreenBackground = AppImageBackground  // #171021